# Generated by nanio-orchestrator for node: {{ member_address }}
# Pool: {{ pool_name }} (nanio, shared storage)
[server]
data_dir = "{{ data_dir }}"
host     = "{{ nanio_host }}"
port     = {{ nanio_port }}
region   = "{{ nanio_region }}"
{% if access_key %}

[[users]]
access_key = "{{ access_key }}"
secret_key = "{{ secret_key }}"
{% endif %}
